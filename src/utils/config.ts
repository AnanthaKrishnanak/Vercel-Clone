import dotenv from "dotenv";

dotenv.config();

export const ACCESS_KEY_ID = process.env.ACCESS_KEY_ID;
export const SECRET_ACESS_KEY = process.env.SECRET_ACCESS_KEY;
export const ENDPOINT = process.env.ENDPOINT;
